import { RiSearchLine } from "react-icons/ri";
import { ICON_STYLE } from "@/components/header/Header";

const SearchIcon = () => {
  return <RiSearchLine {...ICON_STYLE} />;
};

export default SearchIcon;
