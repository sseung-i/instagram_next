import { RiSearchLine } from "react-icons/ri";
import { ICON_PROPS } from "@/components/header/Header";

const SearchIcon = () => {
  return <RiSearchLine {...ICON_PROPS} />;
};

export default SearchIcon;
