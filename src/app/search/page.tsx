import Search from "@/components/search/Search";
import SearchList from "@/components/search/SearchList";

const page = () => {
  return (
    <section>
      <Search />
    </section>
  );
};

export default page;
